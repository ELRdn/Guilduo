{
  "name": "questforge-prototype",
  "version": "0.3.0-social-beta",
  "description": "Open task RPG with MCP, REST, social parties, integrations, and deterministic command battles.",
  "license": "AGPL-3.0-only",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check questforge-core.js && node --check i18n.mjs && node --check service-worker.js && node --check shared/battle-rules.mjs && node --check worker/src/index.mjs && node --check worker/src/security.mjs && node --check worker/src/social-store.mjs && node --check worker/src/oauth.mjs && node --check worker/src/integrations.mjs && node --check worker/src/integration-store.mjs && node --check worker/src/provider-oauth.mjs && node --check mcp-local/questforge-mcp.mjs && node --check tools/update-api-contracts.mjs",
    "deploy": "firebase deploy --only hosting,database",
    "worker:dev": "wrangler dev --port 8787",
    "worker:deploy": "wrangler deploy",
    "mcp:local": "node mcp-local/questforge-mcp.mjs",
    "api:generate": "node tools/update-api-contracts.mjs"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "intl-messageformat": "^11.2.13"
  },
  "devDependencies": {
    "vite": "^8.1.4",
    "wrangler": "^4.26.0"
  },
  "engines": {
    "node": ">=22"
  }
}
